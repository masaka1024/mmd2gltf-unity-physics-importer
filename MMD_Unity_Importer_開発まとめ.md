# MMD glTF → Unity 統合インポーター 開発まとめ

最終更新：2026年7月12日

このドキュメントは、Unity側で物理演算（剛体・ジョイント・揺れ物）とマテリアル（トゥーン/スフィア復元）を自動構築するEditor拡張の開発経緯をまとめたものです。中断・再開時はまずこのファイルを読めば、経緯と現状が把握できるようにしてあります。

---

## 1. プロジェクトの全体像

- **入力**：`mmd2gltf-gui`（別プロジェクト。PMX/VMDをglTF 2.0(.glb)へ変換する自作Pythonツール）が出力した `.glb` ファイル
- **本ツールの役割**：UniGLTFでUnityにインポートされたモデルに対し、`.glb`の`extras`領域に温存されているMMD固有データ（物理演算設定・トゥーン/スフィアマテリアル設定）を読み取り、Unity上で動く形（Rigidbody・ConfigurableJoint・lilToonマテリアル）へ変換する
- **設計思想**（`mmd2gltf-gui`側）：glTFを「MMDアセットを保存する入れ物」として使い、`extras.mmd`に忠実保存レイヤーを持たせ、物理エンジンの無いビューア向けにはベイク済みキーフレームレイヤーも用意する、という2層構造

## 2. 現在のファイル構成（3本、すべて最新）

| ファイル | 配置場所 | 役割 |
|---|---|---|
| `MmdPhysicsImporterWindow.cs` | `Assets/Editor/` | メインのEditor拡張。GUI・JSON解析・剛体/ジョイント生成・マテリアル変換のすべてを含む本体 |
| `PhysicsGltfData.cs` | `Assets/Editor/`以外（通常フォルダ） | `RigidBodyData`・`JointData`のデータクラス定義 |
| `MmdPhysicsImportIndex.cs` | `Assets/Editor/`以外（通常フォルダ） | 生成したコライダー子オブジェクトに付ける目印コンポーネント（`absoluteDataIndex`・`boneName`を保持） |

**旧ファイル（削除可）**：`MmdPhysicsWarmup.cs`・`MmdPhysicsBoneWriteback.cs` — 初期の「専用剛体オブジェクト＋毎フレーム書き戻し」方式で使っていたが、mmd-for-unity方式への全面移行により不要になった。

**同梱の共有トゥーン画像**：`MMD_SharedToon/toon01.bmp`〜`toon10.bmp` — MMD標準の共有トゥーン画像10枚。Assets内のどこかに配置しておくと、`toonShared`を使うマテリアルが自動的に見つけて復元される（ファイル名一致で検索するため配置場所は問わない）。

### 導入前提

- UniGLTFがプロジェクトに導入済みで、`.glb`がUnityにインポートされ、Sceneにモデルが配置されていること
- **lilToon**（URP対応トゥーンシェーダー、v2.3.3で確認）がプロジェクトに導入済みであること（マテリアル変換ボタンを使う場合）

### 使い方（ボタンの実行順）

1. 対象モデルをSceneから選択してウィンドウの「対象モデル」欄にセット
2. **ボタン1**（剛体とコライダーを配置）→ **ボタン2**（ジョイントを結合）の順で必ず実行
3. **ボタン3**（マテリアルをlilToonへ変換）は物理と独立しているので、いつでも実行可能
4. モデルの選択やスクリプトの再コンパイルを挟んだ場合、剛体番号の対応表はエディタ内部の一時状態なので、ボタン2だけ単独では動かないことがある（その場合はボタン1からやり直す）

---

## 3. 開発の経緯（時系列）

### 3.1 物理演算：剛体・ジョイントの構築

**最初の壁：JSONキー名が想定と食い違う**
仕様書ドキュメントに書かれていたフィールド名（`position`/`rotation`/`type`など）は、実際のJSONでは全部スネークケースで、名前も違っていた（`pos`/`rot`/`shape`(int: 0=球,1=箱,2=カプセル)など）。以後、**「仕様書は参考程度、実データを必ずダンプして確認してから実装する」**という方針で進めた。これは物理・マテリアル両方で何度も助けられた教訓。

**ボーンの紐付け問題**
剛体データの`bone`（ボーン番号）を、Unityの`GetComponentsInChildren<Transform>()`が返す配列の並び順としてそのまま使うと、**並び順がglTFの想定と一致せず、誤ったボーンに紐づく**。最終的に、glTFの`nodes`配列を解析して「番号→本当のノード名」の対応表を作り、名前の完全一致で照合する方式に落ち着いた（剛体名からの推測はあくまで次善策）。

**座標変換**
`pos`/`rot`はモデル自身のローカル原点基準の値で、`targetPrefab.transform`（モデルのルート変換）を経由して初めて正しいワールド座標になる。スケールもMMD原寸（1単位≒8cm程度）のままなので、`mode==0`剛体のボーン位置比から自動推定して換算する処理を入れた。

**大きな設計転換：spring-bone書き戻し方式 → mmd-for-unity方式**
当初、「揺れる剛体を専用オブジェクトとして作り、毎フレームその姿勢をボーンへ書き戻す」方式（spring-bone writeback）を実装したが、以下2つの重大なバグに遭遇：

1. 剛体をボーンの子オブジェクトにすると、「ボーンを回転→子である剛体の世界座標が動く→物理エンジンがワープと誤認→暴れる→その結果をまたボーンに書き戻す」というフィードバックループになる
2. 書き戻しの基準に「今のボーンの回転」を直接使うと、前フレームに自分が書いた値を参照する自己参照ループになり発散する

これを受けて、有名なオープンソース参考実装 **mmd-for-unity** のソースを実際に読み、根本的に異なる設計だと判明：**Rigidbody・ConfigurableJointを「メッシュを実際に動かすボーンTransformそのもの」に直接付与し、コライダー形状だけを子オブジェクトに分離する**方式。同じボーンに複数の剛体データが乗る場合は質量で加重平均。この方式へ全面移行し、書き戻し・Warmupの仕組みは丸ごと不要になった。

**mmd-for-unity方式でも引き継いだ弱点**
「単純な球/箱/カプセルのコライダーが、実際のメッシュ形状と一致しない」という、この手法自体の限界を引き継いだ：

- `MMDPhysics`レイヤー全体の自己衝突を無効化していたため、髪が顔を突き抜ける
- スカートと体の衝突を有効化した後、めり込む

対策として、髪・スカートをそれぞれ専用レイヤー（`MMDPhysicsHair`/`MMDPhysicsSkirt`）に分離し「同じ素材同士は衝突なし、体（`MMDPhysics`）とは衝突あり」に設定、コライダーサイズを素材ごとに縮小（スカート0.6倍、髪0.85倍）、衝突再有効化に伴う急激な弾けを防ぐため`maxDepenetrationVelocity`を制限、で緩和している。**この方式は暫定採用であり、最終方針としては髪とスカートで異なるアルゴリズムを使う考え（例：スカートは将来的にクロスシミュレーションへ置き換え）。**

角度制限（`rot_min`/`rot_max`）はMMDの直立基本ポーズ基準に作られており狭いため、ダンス等で脚や腰の向きが大きく変わると衝突・張り付きが起きやすい。現状は全方向に45度の「遊び」を追加して緩和している。

### 3.2 マテリアル：lilToonへの変換

**シェーダー選定**
実際のワークフロー（VRChat/個人勢のアバター制作）で使われているのは`lilToon`と`Poiyomi`が二大巨頭。Poiyomiと`Flat Lit Toon` MMD拡張版はBuilt-in Render Pipeline専用でURP非対応と判明（プロジェクトはURP）。Built-in→URP改造は車輪の再発明と判断し、**lilToon**（v2.3.3、URP対応、スフィア加算/乗算・トゥーンテクスチャを標準機能として持つ）を採用した。

**アセンブリ参照の壁**
`lilToon.Editor`アセンブリは`autoReferenced: false`に設定されており、素直に`using lilToon;`すると コンパイルエラーになる。アセンブリ定義ファイルを新設する代わりに、**型名を文字列で指定するリフレクション**で`lilToonInspector.SetupMaterialWithRenderingMode`（公開API）を呼ぶ方式に変更し解決。

**実データ確認**
`extras.mmd.materials`の実フィールド名は**キャメルケース**（`sphereMode`/`sphereTexture`/`toonTexture`/`toonShared`。rigidBodies/jointsのスネークケースとは異なる）。`sphereTexture`/`toonTexture`はglTFの`textures`配列の番号だが、**Unity側にサブアセットとしてインポートされる順序はglTFの配列順と一致しない**（実物で確認済み）。名前は完全一致するため、必ず名前で照合する。

**UniGLTFが画像を取り込まない問題**
UniGLTFは、標準glTFマテリアルのスロット（`baseColorTexture`等）から参照されていない画像（＝トゥーン/スフィア専用画像）は、Unityのテクスチャアセットとして作らない。そのため、**GLBバイナリの`bufferViews`を直接辿って生のPNGバイト列を切り出し**、`Assets/(モデルと同じフォルダ)/MMD_ExtractedTextures/`に新規テクスチャアセットとして保存・インポートする処理を追加した。

**黒つぶれ事件**
変換直後、モデル全体が真っ黒になる不具合が発生。ライト強度などの照明周りを疑ったが的外れで、原因は**`sphereMode`の「加算」「乗算」の対応が、仕様書記載と実際で逆だった**こと。乗算合成でハイライト用スフィアテクスチャがベースカラーを黒く潰していた。標準PMX仕様の並び（1=乗算, 2=加算）に修正して解決。トゥーン/スフィア復元・見た目とも正常動作を確認済み。

**未対応の既知の制限**
`toonShared`（MMD共有トゥーン、toon00〜10.bmp）を使うマテリアルは、画像自体がglTFに埋め込まれていないため復元できない（警告ログを出すのみ）。

---

## 4. 現状のステータス（2026年7月12日 更新）

- ✅ 剛体・コライダー生成、ジョイント結合（mmd-for-unity方式）
- ✅ 髪・スカートの衝突レイヤー分離とめり込み緩和
- ✅ lilToonへのマテリアル変換（ベースカラー・アルファ・両面描画）
- ✅ トゥーンテクスチャ・スフィアマップの復元（GLBバイナリからの自前抽出込み）
- ✅ 輪郭線（アウトライン）の復元（`flags`ビット・`edgeColor`・`edgeSize`から）
- ✅ 共有トゥーン（`toonShared`、MMD標準のtoon01〜10.bmp）の復元。ユーザーが用意した10枚をプロジェクト内から名前で自動検索して割り当て
- ✅ **調整パネル**を追加。剛体の質量/減衰の最低値、関節ばねの最低値、角度制限の遊び、髪/スカートのコライダー縮小率、めり込み解消速度の上限、輪郭線太さの換算係数――これまでコードに埋め込んでいたマジックナンバーをすべてウィンドウ上のスライダーから調整可能に
- ✅ 複数モデル（機械的な素材のキャラ、Tda式初音ミクV4X等）で動作確認済み
- ⚠️ 髪の細かい震え・スカートの厚み感は、単純コライダーの限界によりモデルごとの微調整が必要（→調整パネルで対応）
- ⚠️ `ambient`/`specular`+`specularPower`のマテリアル反映は見送り（lilToonに対応するプロパティが無く、近似実装の効果も軽微と判断）

### 4.1 mmd2gltf-gui（エクスポーター）側で判明した既知の問題

Unity側インポーターとは別に、`mmd2gltf-gui`（Pythonエクスポーター）側でも1件、重要な既知問題が判明した。

**UniGLTFはsparse形式のモーフターゲットを正しく読み込めない**：一部モデル（Tda式初音ミクV4X等）で、口パクモーフを動かすと見た目のサイズが崩れる現象が発生。詳細調査の結果、エクスポートされたデータ自体・Unity側BlendShapesの並び順ともに正常で、頂点変形量にも異常は無いと確認。原因はUniGLTFが`morph_mode="sparse"`（差分圧縮）形式のモーフターゲットaccessorを正しく解釈できないインポート側の制約と特定。`morph_mode="dense"`（フルサイズ形式）で書き出し直すことで解決を確認済み。**今後UniGLTFで使う前提のモデルは、エクスポート時にdenseモードを標準にすることを推奨。**

## 5. 次にやるとしたら

- スカート専用の物理アルゴリズム（クロスシミュレーション）の設計・実装
- 調整パネルの値をプリセットとして保存/読み込みできるようにする
- `ambient`/`specular`の近似反映（要望があれば）
- `mmd2gltf-gui`側のデフォルトを`dense`モーフに変更するか検討
